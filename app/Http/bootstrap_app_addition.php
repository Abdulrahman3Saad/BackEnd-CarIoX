<?php
/**
 * =====================================================
 * هذا الملف مش بيتحمل تلقائياً — محتوياته تُضاف يدوياً
 * لـ bootstrap/app.php في مشروعك
 * =====================================================
 * 
 * في bootstrap/app.php، ابحث عن ->withMiddleware(function (Middleware $middleware) {
 * وأضف داخله:
 *
 *     $middleware->alias([
 *         'admin' => \App\Http\Middleware\AdminMiddleware::class,
 *     ]);
 *
 * =====================================================
 */
